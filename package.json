{
  "name": "zombie-survivor",
  "version": "1.0.0",
  "description": "Juego multijugador de zombies en tiempo real",
  "scripts": {
    "dev": "live-server --port=5500",
    "deploy": "firebase deploy --only hosting,firestore",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "generate:videos": "node generate-videos.js"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
